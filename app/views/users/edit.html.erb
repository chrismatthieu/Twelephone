<br/><h1>Editing Your Profile</h1><br/>

<div id="pagecontentwithlinks">

<div id="photobar">
	<center>
	<% if @user.photo %>	
		<img src="<%= @user.photo %>" width="100" height="100" />
	<% else %>
		<%= gravatar_image_tag @user.email, :class => "gravatar" rescue ""%>
	<% end %><br/>
	<% if @current_user.id == @user.id %>
		<span style="a:underline"><%= link_to 'Edit Profile', "/users/" + @current_user.username + "/edit" %> </span>
	<% end %>
</center>
</div>

Twlephone has integrated gravatars to help put a faces with a names. It’s a free service that associates a picture with your email address. If you plan to be an active Twlephone user, you should sign-up at 
<span style="a:underline;"><%= link_to "http://gravatar.com", "http://gravatar.com"%></span>.

<br/><br/>
<%= render 'profile' %>

</div>